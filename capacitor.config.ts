import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'id.medxa',
  appName: 'medxa-helpdesk',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
